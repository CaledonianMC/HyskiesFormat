# HyskiesFormat configuration file generated using HyskiesFormat-1.3
# This plugin mainly supports 1.8.8, but may work in later versions

# Death Msgs
death:
  enable-death: true

join-leave:
  enable-join: true
  enable-leave: false